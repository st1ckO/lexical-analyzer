 Handle operators
        # if char in OPERATOR:
        #     token, index = handle_operators(inputCode, index, length, previousToken)
        #     tokens.append(token)
        #     previousToken = token
        #     continu